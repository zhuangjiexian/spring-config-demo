server:
  port: 5918
spring:
  profiles:
    #对应config server获取配置文件的{profile}
    active: test
  cloud:
    config:
      uri: http://localhost:5168/
      #对应config server获取配置文件{application}
      name: spring-config-client
      #对应config server获取配置文件的{label}
      label: master
  application:
    name: spring-config-client
