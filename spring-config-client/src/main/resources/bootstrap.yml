server:
  port: 5918
spring:
  profiles:
    active: test
  cloud:
    config:
      uri: http://localhost:5168/
      name: spring-config-client
      label: master
      profile: dev
  application:
    name: spring-config-client
