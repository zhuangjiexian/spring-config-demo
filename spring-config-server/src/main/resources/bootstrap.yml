server:
  port: 5168
spring:
  application:
    name: spring-config-server

  data:
    mongodb:
      database: test
      uri: mongodb://username:password@localhost:3717/?authSource=test
      auto-index-creation: false