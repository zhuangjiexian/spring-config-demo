server:
  port: 5168
spring:
  application:
    name: spring-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhuangjiexian/spring-cloud-config-repo
          username: username
          password: password
